import { useState } from 'react';  const mockData = {   contacts: [     { id: 1, name: 'ООО "Ромашка"' },     { id: 2, name: 'ИП Иванов' },   ],   chat: [     { from: 'client', text: 'Добрый день! Интересует ваш продукт.' },     { from: 'manager', text: 'Здравствуйте! Рад помочь. Что именно вас интересует?' },   ],   details: {     name: 'ООО "Ромашка"'   } };  export default function DemoWorkspace() {   const [selectedContact, setSelectedContact] = useState(mockData.contacts[0]);    return (     <div className="grid grid-cols-4 gap-4 h-[600px]">       <div className="col-span-1 bg-gray-100 p-4 rounded">         <h2 className="font-bold mb-4">Контакты</h2>         <ul>           {mockData.contacts.map(contact => (             <li key={contact.id}                  className={`cursor-pointer p-2 rounded ${selectedContact.id === contact.id ? 'bg-blue-200' : ''}`}                 onClick={() => setSelectedContact(contact)}>               {contact.name}             </li>           ))}         </ul>       </div>       <div className="col-span-2 bg-gray-100 p-4 rounded">         <h2 className="font-bold mb-4">Чат с клиентом</h2>         <div className="space-y-2">           {mockData.chat.map((message, i) => (             <div key={i} className={`p-2 rounded w-3/4 ${message.from === 'client' ? 'bg-white' : 'bg-blue-100 ml-auto'}`}>               {message.text}             </div>           ))}         </div>       </div>       <div className="col-span-1 bg-gray-100 p-4 rounded">         <h2 className="font-bold mb-4">Детали</h2>         <p>{selectedContact.name}</p>       </div>     </div>   ); }