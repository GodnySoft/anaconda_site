# Workspace Page

## URL

`/workspace`

## Purpose

Show ANACONDA as an operational company workspace: communications, tasks, approvals, invoices, system events, and control context in one environment.

## Scope

The page is a demo/future-ready UI architecture. It is not a production realtime chat.

## CTA

The page routes to audit lead capture with `source_page=workspace`.

